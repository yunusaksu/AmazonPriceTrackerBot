# AmazonPriceTrackerBot
A Python automation bot that tracks product prices on Amazon using web scraping. It monitors selected products at regular intervals and stores price history in a JSON file. When a price changes (especially drops), the bot automatically sends an email notification to the user.  Built with Selenium, SMTP email integration, and designed for continuous
